= 总结与展望

== 总结

本文通过完善的基础设施支持，设计了一款四级流水线顺序单发射32位RISC-V处理器。将其接入SoC平台，在配套的运行时环境下进行外设的功能验证，通过指令测试集并能成功启动RT-Thread。并在类似流片的环境下针对嵌入式场景进行了性能分析与优化，在面积与性能之间进行了权衡。

具体来说，本文主要工作如下：

一是根据RISC-V指令集手册描述，实现了一个单周期处理器的原型，其中包含了RV32E规定的用户级架构状态与对应的指令，以及部分特权架构状态与指令。暂时对访存接口进行简化，在Verilator仿真环境中实现对应的设备，即在功能性上正确，能够正确执行编译到RV32E架构的RT-Thread操作系统，但不能提供精确的周期级模拟。

二是学习NEMU中的设计，在Verilator仿真环境中搭建类似的基础设施，实现NPC的调试器。从而获得控制NPC状态机的能力，提供了指令级，函数级等更高抽象层级的视角，与周期级的波形图结合，在实际调试中可以灵活切换，自顶向下地在不同的抽象层级中观测状态机。并在NEMU和NPC之间搭建Difftest模块，实现程序执行时的指令级动态检查，使状态错误在发生时尽快地暴露出来，在开发过程中减少了大量调试的时间。

三是实现访存模块，接入SoC环境。由于NPC中存在多主设备与多从设备，搭建了Arbiter和Xbar来进行仲裁和路由。接入ysyxSoC环境，构建处理器核与各种设备之间的互联通路，实现包括内存，持久化存储等关键设备的仿真模型，提供了更接近于真实芯片运行环境的精确仿真。并根据SoC环境的结构实现配套的运行时环境与构建脚本，使得程序能够以较低的开销移植并正确运行在NPC上，并在SoC环境中进行了NPC的硅前验证。

四是在上文精确的仿真环境中，以Microbench为基准程序，对基准程序本身的性质进行了评估，并通过流水线，cache等技术优化了处理器架构设计，使得其能够匹配基准程序的性质，从而提高性能。并针对嵌入式场景对面积和时序进行了优化，最终主频为500MHz,面积为26710.39$mu m^2$。

== 展望

本文实现了RISC-V指令集处理器以及其配套SoC系统的设计与验证，但在整体设计上仍存在许多局限性，有许多地方存在优化空间：

一是处理器微架构方向的优化。根据性能计数器结果可以看到，当前流水线的CPI相对于多周期仍有许多进步空间。由于流水线处理器中事件存在重叠，为了理解处理器性能的构成，需要重新设计性能计数器#[@interval]，加入优先级等逻辑以得到更精确的CPI构成，从而在面积的限制下针对指令供给，数据冒险与控制冒险进行精准的优化，比如通过分支预测与缓存预取等技术进一步提高处理器性能，或后续改成乱序处理器架构，其关键在于通过精准的性能计数器架构寻找到实际的性能瓶颈。

二是可以进行指令集的完善，实现更完善的特权架构，结合AM模块的进一步扩展，比如支持虚拟内存，启动xv6或Linux等更复杂的程序。

三是可以将NPC与SoC移植到FPGA上进行上板验证，提高验证效率，实现更完善的硅前验证链，为后续的流片提供保障。

四是可以使用基于Scala的领域特定语言Chisel#[@chisel]对处理器核进行重构，结合现代编程语言的能力提高处理器开发的效率，并针对设计与验证的工具链进一步探索，实现敏捷开发。